import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "tech.ayuntamiento.munitrans",
  appName: "munitrans-app",
  webDir: "dist",
};

export default config;
